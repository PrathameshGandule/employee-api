# employee-api
A simple Employee CRUD API made using node.js, express.js and MySQL 

- make sure to add `.env` file with following info
```bash
PORT=
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_DATABASE=
```
```bash
npm install
npm run start
```
